# define your env variables for the test env here
KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rt3st'

# Default: a separate "<db>_test" database (standard isolated test DB). Override to empty
# in .env.test.local on a machine that can't create one (runs against the main DB instead).
TEST_DB_SUFFIX=_test
