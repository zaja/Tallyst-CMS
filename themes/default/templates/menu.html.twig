{# Menu wrapper for render_menu(location). Override in a child theme by placing your
   own themes/<name>/templates/menu.html.twig. #}
<nav class="site-nav" aria-label="{{ location }}">
    {{ include('menu_items.html.twig', {items: items}) }}
</nav>
